/**
 * Chromatic modes for visual regression testing.
 * Each mode captures snapshots with different theme settings.
 */
export const allModes = {
  light: {
    theme: 'light',
  },
  dark: {
    theme: 'dark',
  },
}
